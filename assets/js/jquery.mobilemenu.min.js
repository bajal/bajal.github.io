/*
 * Mobile Menu - jQuery Plugin
 * Simple mobile menu for responsive websites
 */
(function($){$.fn.mobileMenu=function(options){var defaults={defaultText:'Navigate to...',className:'select-menu',subMenuClass:'sub-menu',subMenuDash:'&ndash;',topOptionText:'Menu',prependTo:'.header',switchWidth:600};var settings=$.extend(defaults,options);var topOption='';if(settings.topOptionText){topOption='<option value="">'+settings.topOptionText+'</option>';}
return this.each(function(){var $obj=$(this);var output='<select class="'+settings.className+'">';output+=topOption;$obj.find('li').each(function(){var $this=$(this);var padLeft='';var address=$this.children('a').attr('href');var text=$this.children('a').text();if($this.parents('ul').length==2){padLeft=settings.subMenuDash+' ';}
if(address!=undefined){output+='<option value="'+address+'">'+padLeft+text+'</option>';}});output+='</select>';$(settings.prependTo).prepend(output);$obj.addClass('original-menu');$(settings.prependTo).find('.'+settings.className).change(function(){var locations=$(this).val();if(locations!==''){window.location.href=locations;}});});}})(jQuery);

